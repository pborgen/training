[project]
name = "knowledge"
version = "0.1.0"
description = "Scrapes workout knowledge from the web into Postgres for later processing"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "selectolax>=0.3.25",
    "trafilatura>=1.12.0",
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.0",
    "tenacity>=9.0.0",
]

[project.scripts]
knowledge = "knowledge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/knowledge"]
